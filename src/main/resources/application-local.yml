# Spring Configuration
spring:
  config:
    activate:
      on-profile: local

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/gather_here?useSSL=false&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=true
    username: root
    password: gatherhere1234

  jpa:
    hibernate:
      ddl-auto: update

  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 100MB

logging:
  level:
    org:
      hibernate:
        SQL: debug

# Security Configuration
security:
  salt: 10billion

  jwt:
    secret: 6rmA7IKw7ZWY6rmA7KKF66+86rmA64+E7Jew7J2Y7LKr7ZSE66Gc7KCd7Yq47ISx6rO16riw7JuQ
    access-token:
      minute: 1
      prefix: Bearer
      header:
        name: Authorization

    refresh-token:
      minute: 1
      prefix: Bearer
      header:
        name: Refresh-token

redis:
  host: localhost
  port: 6379

aws:
  access:
    key: null
  secret:
    key: null